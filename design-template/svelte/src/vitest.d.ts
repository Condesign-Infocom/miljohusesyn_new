// Registers @testing-library/jest-dom matchers (toBeInTheDocument, toHaveClass,
// toHaveAttribute, …) with vitest's Assertion types so svelte-check recognizes them.
import '@testing-library/jest-dom/vitest';
